program Cale1;

var float boba, memo, santi, pucha;
var int saof, paro2;
var bool si, no;

func int condi(){

    memo = 6 / (5 + 8) * 2;
    si = memo > 4 && 1 <= 3 || memo < 8 && paro2 != pucha;
    return si;
}

void main(){
}